<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.personalclouddisk">

    <!-- 权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <!-- Android 13 (API 33) 通知权限 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PersonalCloudDisk"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".ui.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.PersonalCloudDisk.NoActionBar" />

        <!-- 修改这里：从Theme.AppCompat.Light.NoActionBar改为Theme.PersonalCloudDisk.NoActionBar -->
        <activity
            android:name=".ui.RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.PersonalCloudDisk.NoActionBar" />

        <activity
            android:name=".ui.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.PersonalCloudDisk.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.ShareActivity"
            android:exported="false"
            android:theme="@style/Theme.PersonalCloudDisk.NoActionBar"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.ProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.PersonalCloudDisk.NoActionBar"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.AboutActivity"
            android:exported="false"
            android:theme="@style/Theme.PersonalCloudDisk.NoActionBar"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.PersonalCloudDisk.NoActionBar"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.SelectFolderActivity"
            android:exported="false"
            android:theme="@style/Theme.PersonalCloudDisk.NoActionBar"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.DownloadActivity"
            android:exported="false"
            android:theme="@style/Theme.PersonalCloudDisk.NoActionBar"
            android:parentActivityName=".ui.MainActivity" />
    </application>

</manifest>